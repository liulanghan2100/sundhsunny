# 审计报告

审计时间：2026-09-16
审计范围：agentos-hub 全包
审计方式：全命令实跑 + 静态扫描 + 实测验证

---

## 总评

**功能层面健康**：15 个命令全部零报错，自检 24/24、单测 9/9。
**结构层面有冗余**：存在若干"带过来但入口用不到"的部件。
**发现 1 个真逻辑漏洞**（图谱检索项目分配不均）。

按严重程度排列如下。

---

## 一、真漏洞（建议修）

### L1 图谱检索：只查到一个项目就停

**位置**：`hub.py` `graph_search()` 第 466-467 行

```python
for it in items[:limit]:
    hits.append({"project": name, "item": it})
if len(hits) >= limit:      # <- 这里
    break
```

**问题**：按项目顺序逐个查，**凑够 limit 条就跳出循环**，后面的项目根本没查。

**实测证据**：

| 查询 | limit | 涉及项目数 |
|---|---|---|
| Applier | 8 | **1 个**（应为 4 个）|
| Applier | 40 | **1 个** |
| Applier | 200 | **1 个**（返回 48 条仍只 1 个项目）|

`graph_search("Applier")` 永远只返回 `OpenNiss.TiaExport.V21` 的结果，
而 `MCPCreate20260719`（39430 节点的主库）从未被搜到。

**影响**：搜代码时可能漏掉主库里的相关实现，而你无从察觉。

**修法建议**：改成每个项目各取一部分，或查完所有项目再按相关度排序截断。

---

## 二、逻辑缺陷（建议改）

### L2 图谱项目顺序不稳定，影响可复现性

缓存里有 4 个项目，第 1 个是**测试残留** `C-Users-sundh-AppData-Local-Temp-_nametest-MyProject`（18 节点）。
它排在列表最前，会被优先搜索。

**影响**：搜索结果的偏向取决于缓存里项目的排列顺序，不稳定。

### L3 配置项有 8 个写了但没被读取

| 配置项 | 状态 |
|---|---|
| `layers.compat` | 未使用 |
| `knowledge.trusted_dir` | 未使用（实际路径写死在别处）|
| `knowledge.quarantine_dir` | 未使用 |
| `skills_config.shadow` | 未使用 |
| `skills_config.match_fields` | 未使用（代码里是硬写的）|
| `gates.canonical` | 未使用 |
| `graph.provider` | 未使用（仅备注）|

**影响**：改这些配置不生效，容易误导。

### L4 知识库路径三处不一致

- `config.yaml` 有 `knowledge.trusted_dir` / `quarantine_dir`
- 但 `recall_knowledge()` 实际用的是环境变量 `AGENTOS_HUB_KB_DIR`
- `engine/knowledge/knowledge-base/scripts/kb_common.py` 里又有自己的路径推导

**影响**：改配置不一定生效。

---

## 三、冗余（可清理，不影响功能）

### R1 图谱缓存有 1.7MB 测试残留

```
C-Users-sundh-AppData-Local-Temp-_nametest-MyProject.db   1.7 MB
```

这是之前测"项目名怎么派生"时留下的，可以删。

### R2 engine/mcps 下 15 个模块入口完全不用

`engine/mcps/` 有 16 个模块（147 文件，1.60 MB），
**入口 `hub.py` 一个都没引用**。它们只被彼此引用，
来自源库的"Agent OS 完整版"，在 hub 里属备用件。

**注意**：不是垃圾——`task_queue_mcp` 被治理核测试引用，
`dashboard_mcp` 等有实际功能。只是主流程不经过它们。

**建议**：不必删，但在 `DIRECTORY.md` 里注明"备用件，主流程不经过"。

### R3 已归档技能占 4.64MB

`skills/skills-shared-candidate/` 下 24 个已归档技能，占 4.64MB。
可以移到 `_archive/`，但删了也无大碍。

### R4 两个 `_shared` 目录内容相同

```
engine/mcps/_shared/     56 文件
engine/memory/_shared/    4 文件   <- io.py / time.py / state.py 与 mcps 版逐字节相同
```

**影响**：改一处不会同步另一处。属轻微重复。

### R5 重复目录（源库遗留）

| 重复项 | 正本 | 副本 |
|---|---|---|
| manual-gates | `skills/manual-gates/` | `skills/skills-shared-candidate/manual-gates/` |
| knowledge-base | `engine/knowledge/` | `skills/knowledge-base/` |

副本内容与正本**基本不同**（脚本有差异），不是简单复制。

---

## 四、文档不符（建议更新）

### D1 README 记忆条数过期

README 说 458 条，实际 **457 条**（清理测试记录时减了 6 条、又新增过）。

### D2 README 有 2 处路径写错

| README 写的 | 实际 |
|---|---|
| `manual-gates/common.py` | `manual-gates/scripts/manual_mcp/common.py` |
| `tools/.../cache` | 路径简写，可能引起误解 |

### D3 README 「已知边界」内容过期

里面写"index_repository 在部分环境下会崩溃"，但**已找到解法**
（两个环境变量成对给）。这条需要更新。

---

## 五、缺失（建议补）

### M1 核心能力未接：审批 / 校验 / 恢复

`core/Agent_OS_Core/` 下这三组模块**都能独立工作**（实测通过）：

| 模块 | 能力 | 实测结果 |
|---|---|---|
| `governance/owner_gate.py` | 生成决策记录、校验审批 | 能生成、能校验 |
| `recovery/policy.py` | 选恢复策略 | 9 种失败类型都有策略 |
| `recovery/rollback.py` | 生成回滚计划 | 能生成，强制要求 owner 门 |
| `recovery/incident.py` | 生成事故单 | 能生成 |
| `schemas/validation.py` | 任务卡校验 | 能校验 |

**但 `hub.py` 一个都不调用**。它们是"任务执行后"的环节（审批留痕、失败恢复、回滚），
与当前"任务进来先分级"的定位不同。

### M2 缺仓库基础文件

- `LICENSE` —— 无
- `.editorconfig` —— 无

（内部自用包，非必需）

### M3 `hub.py` 有 17 处宽泛异常捕获

其中 4 处是 `except Exception: pass`（静默吞掉）。
**审查后确认是"优雅降级"设计**，不是漏洞——比如技能路由失败不影响主流程。
但建议加日志，否则出问题无从排查。

---

## 六、命名与结构

### N1 `skills-shared-candidate` 名字骗人（已知）

名字说"候选"，实际装 **35 个主力技能**。
已在 `DIRECTORY.md` 说明，改动风险大于收益，保持现状。

### N2 `engine/knowledge/` 里嵌了 `knowledge-base/` 两层

```
engine/knowledge/knowledge-base/scripts/
```

中间多一层，是因为源库里就这样。可扁平化为 `engine/knowledge/scripts/`，
但要同步改 `hub.py` 两处引用。

### N3 命名风格不统一

| 风格 | 数量 |
|---|---|
| kebab-case | 64 |
| snake_case | 21 |
| 其他 | 103 |

主要是 Python 包（snake_case）与技能名（kebab-case）混用，属正常现象。

---

## 七、验证记录

### 全命令实跑（15 项，全部零报错）

| 命令 | 退出码 | 耗时 |
|---|---|---|
| doctor | 0 | 5.0s |
| selftest | 0 | 2.7s |
| stats | 0 | 2.6s |
| version | 0 | 0.7s |
| constitution | 0 | 0.4s |
| graph list_projects | 0 | 2.6s |
| skills | 0 | 0.4s |
| memory | 0 | 5.0s |
| kb | 0 | 0.6s |
| recall | 0 | 14.5s |
| learn | 0 | 2.7s |
| gate | 0 | 5.3s |
| ask（常规）| 0 | 18.0s |
| ask（高危）| 0 | 17.7s |
| ask（违宪）| 0 | 12.5s |

### 分级判定正确

| 任务 | 判定 |
|---|---|
| 改 charter.json 红线 | L3 + BLOCK 拦截 |
| 部署上线并转账100万 | L3 + 需审批 |
| 整理文档 | L1 |

### 边界情况

| 场景 | 结果 |
|---|---|
| 空任务描述 | L2 + pending_approval（保守，合理）|
| `evidence_limit=0` | 正确不回传 |
| `skill_limit=0` | 正确不回传 |
| 知识库空查询 | 返回空列表，不报错 |

---

## 八、整改优先级建议

| 优先级 | 项目 | 理由 |
|---|---|---|
| **高** | L1 图谱检索项目分配 | 真漏洞，会静默漏结果 |
| **高** | D1/D2/D3 文档更新 | 文档不可信会误导 |
| **中** | R1 删测试残留缓存 | 顺手，减少干扰 |
| **中** | L3/L4 配置项对齐 | 避免"改了不生效" |
| **低** | R2/R3/R4 冗余清理 | 不影响功能 |
| **低** | M1 接审批/恢复 | 取决于是否要走完整闭环 |

---

## 九、不改的部分（说明理由）

以下看起来"可优化"，但**建议保持现状**：

1. **`skills-shared-candidate` 改名** —— 牵涉技能登记表、别名表、图谱索引，风险大于收益
2. **`engine/mcps` 15 个备用模块** —— 体积仅 1.6MB，删了可能影响治理核测试
3. **17 处宽泛异常捕获** —— 是刻意的降级设计，删了反而更脆
4. **两个 `_shared` 重复** —— 合并需要改动多个 MCP 的导入路径
