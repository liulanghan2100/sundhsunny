# 备用能力清单

`engine/mcps/` 下有 15 个功能模块。**主流程（`hub.py ask`）不经过它们**，
但每个都能独立启动，需要时可以调研、取用。

这份清单回答一个问题：**"我想做某件事，包里有没有现成的？"**

---

## 怎么用

### 看有哪些

```
python hub.py mcps                     列出全部模块
python hub.py mcps queue               看某个模块的细节
```

### 单独启动一个模块

```python
import sys
sys.path.insert(0, "engine/mcps")
sys.path.insert(0, "engine/memory")   # task_queue 需要
sys.path.insert(0, "core")            # task_queue 需要
import task_queue_mcp.common as c
server = c.build_server()
```

> **注意依赖路径**：`task_queue_mcp` 依赖记忆引擎和治理核，
> 上面三行 `sys.path` 缺一不可（实测少了会报 `ModuleNotFoundError`）。
> 其余 14 个只需要第一行。

---

## 模块一览

按"你可能想做什么"分组。

### 一、任务治理类

| 模块 | 它能做什么 | 工具数 |
|---|---|---|
| `control_plane_mcp` | 策略检查、自主权决策、启动受治理任务、追加审计事件 | 8 |
| `mandatory_runtime_hook_mcp` | 强制路由规划、合规校验 | 5 |
| `preflight_bundle_mcp` | 执行前统一检查，记录检查包 | 3 |
| `side_effect_admission_mcp` | 副作用准入：申请准入票、校验票据 | 4 |
| `capability_profile_mcp` | 定义能力画像、检查某动作是否被允许 | 4 |

**什么时候想起它**：想把"什么能做、什么不能做"从配置文件升级成可查询的服务。

### 二、任务队列类

| 模块 | 它能做什么 | 工具数 |
|---|---|---|
| `task_queue_mcp` | 队列状态、入队、自启动治理、执行证据 | 20 |

**什么时候想起它**：想让任务排队自动跑，而不是一条条手动发起。

> 注：该模块体量最大（142KB），功能也最完整，但依赖最多。

### 三、验证与复盘类

| 模块 | 它能做什么 | 工具数 |
|---|---|---|
| `completion_verifier_mcp` | 按证据验证完成度，签发完成证书 | 3 |
| `failure_replay_mcp` | 记录失败样本、蒸馏教训、检索相似失败、建规避计划 | 7 |
| `compiled_correction_mcp` | 记录用户纠正、编译成检查规则、按规则校验 | 5 |

**什么时候想起它**：想给"做完了"一个客观判据，或想从失败里系统性地学。

### 四、记忆增强类

| 模块 | 它能做什么 | 工具数 |
|---|---|---|
| `memory_consolidation_mcp` | 把记忆分层（语义/情景/程序）、检测冲突、健康度报告 | 6 |
| `dashboard_mcp` | 采集数据、生成看板 HTML | 6 |

**什么时候想起它**：记忆条目多了、想整理；或者想有个可视化的运行视图。

> `dashboard_mcp` 体积最大（894KB），因为自带前端资源。

### 五、运行时编排类

| 模块 | 它能做什么 | 工具数 |
|---|---|---|
| `runtime_integration_mcp` | 创建并执行集成任务环、导出状态、建备份 | 6 |
| `runtime_middleware_mcp` | 按任务类别搭中间件栈、校验上下文完整性 | 4 |
| `hook_runtime_mcp` | 钩子链：工具调用前后、停止时、事件记录 | 6 |

**什么时候想起它**：想在任务执行前后插入自动化动作。

### 六、任务准入类

| 模块 | 它能做什么 | 工具数 |
|---|---|---|
| `cognitive_intake_mcp` | 把请求分类成 A/B/C/D、建执行卡、评估后果 | 6 |

**什么时候想起它**：想要比当前 L1/L2/L3 更细的任务分级。

**和现有能力的关系**：`hub.py ask` 已经用治理核的
`unified_intake` 做了分级（L1/L2/L3）。这个模块分类更细（A/B/C/D），
且有执行卡机制，可以作为进阶方案调研。

---

## 与主流程的关系

```
hub.py ask  ──> 治理核(policy) ──> 记忆/知识/图谱 ──> 简报 ──> 门禁
                     │
                     └── 主流程用到 here

engine/mcps/*  ──> 全部在此之外，独立可用
```

**为什么不直接接进来**：这些模块来自源库的"Agent OS 完整版"，
设计前提是有一整套运行时（队列守护进程、钩子注入点、中间件框架）。
当前 hub 是轻量编排层，直接接会产生大量耦合。

**它们的价值**：作为**能力储备**。需要某个功能时，可以：
1. 先单独调研这个模块（用上面的启动方式）
2. 评估它的依赖和影响面
3. 再决定是接入主流程，还是把它的思路借鉴过来自己实现

---

## 状态核查（2026-09-16）

| 项 | 结果 |
|---|---|
| 模块总数 | 15 |
| 可导入 | **15 / 15** |
| 可建 server | 15 / 15 |
| 入口 main 流程引用 | 0 个 |

体积合计 1.60 MB，其中 `dashboard_mcp` 占 894KB、`task_queue_mcp` 占 143KB。
