# backup_integrity_mcp

v6.23 backup integrity manifest for Manual Agent OS.

It creates and verifies local backup manifests:

- backup filename
- size
- modified time
- SHA-256 hash
- optional project/version label

It does not delete, upload, publish, or restore backups.
