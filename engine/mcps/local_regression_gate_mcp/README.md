# local_regression_gate_mcp

v6.17 local regression gate for Manual Agent OS.

It runs safe local checks only:

- Python compilation
- selected smoke tests
- optional policy parse
- JSON/Markdown regression report

It does not push code, open PRs, deploy, install packages, or request admin
permission.
