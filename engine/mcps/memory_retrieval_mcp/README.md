# memory-retrieval-mcp

v6.30 builds a pre-task retrieval packet from existing memory systems:

- keyword memory search
- SQLite FTS search
- local vector hybrid search
- consolidated semantic/episodic/procedural memory search

It writes retrieval packets to
`09_投研/memory_retrieval/retrieval_packets.jsonl`.
