# 变更记录

本文件记录每个版本的**做了什么、为什么做**。
逐次提交的细节看 git 历史；这里只写"值得回看"的部分。

版本号规则：`主版本.次版本.修订号`

- **主版本**：流水线结构变化（新增加一个必经环节）
- **次版本**：新增能力或修复缺陷
- **修订号**：文案、配置、小调整

---

## 1.1.0 — 2026-09-16

这一轮的核心是**把三个断点接上，并修掉两个安全判定的缺陷**。

### 新增能力

**1. 检索结果回传**

原来 `ask` 只给命中数量（`memory_hits: 5`），但 5 条是什么看不到，
要再手动跑一次 `recall` 才能拿到内容——检索等于白做。

现在直接返回压缩摘要（每条 160 字封顶），三个来源各自计条数、互不挤占：

```
"evidence": {
  "memory": { "hits": [{ "id": "...", "ts": "2026-09-12 14:42",
                         "summary": "Add two V21 SINAMICS V90 PN servos", "score": 0.34 }] }
}
```

**2. 技能路由汇入**

`ask` 原本完全不碰技能路由，35 个 active 技能等于隐形。

现在任务描述会反查出可用技能，写进简报和"下一步"：

| 任务描述 | 命中的技能 |
|---|---|
| 帮我审查一下这段代码的改动 | code-review |
| 做一个流程图的文档 | process-doc |
| 把这个方法蒸馏成技能 | nuwa-runbook |

中文没有分词库，用"先剥虚词、再做 n-gram"的方式近似。
（踩过的坑：直接切分会让"一下""这个"这类虚词把无关技能顶到前面。）

**3. 门禁只读预览**

`ask` 会带上门禁状态：这个项目在 27 节点手册里走到哪、下一个待办是什么。
严格只读——实测调用前后项目数都是 244，不创建目录、不写状态。

**4. 宪法层接入**

宪法校验器接进主流程，做两件事：完整性自检 + 路径写入预检。
三档判定：`BLOCK`（路径被拦）> `REVIEW`（提到受保护名字）> `ALLOW`。

**5. 图谱按路径认项目**

图谱的项目名是动态派生的（给了 `--name` 就是那个名字，没给就是绝对路径
slug 化，如 `C-Users-sundh-AppData-Local-Temp-_nametest-MyProject`）。
换机器、换目录，名字就变，写死必废。

新增解析层，允许用**路径**或**名字片段**指定项目：

| 输入 | 解析结果 |
|---|---|
| `C:/Users/.../MCPCreate20260719` | MCPCreate20260719 |
| `.../MCPCreate20260719/AutoRobot` | 那个子项目 |
| `OpenNiss` | OpenNiss.TiaExport.V21 |
| `mcpcreate20260719` | MCPCreate20260719（大小写不敏感）|

### 缺陷修复

**F1 中文动词表缺字**（安全相关）

`帮我改 charter.json 的红线` 判成 L1（免审批）。根因是词表只有双字词
（修改/编辑），漏了口语里更常用的单字"改"、"删"。

修复后：

| 任务 | 改前 | 改后 |
|---|---|---|
| 帮我改 charter.json 的红线 | L1 | **L2** (write) |
| 把 charter.json 删掉 | L1 | **L3** (delete) |
| 替换 charter.json 里的内容 | L1 | **L2** (write) |

**F2 宪法判定不回馈到分级**（安全相关）

同一份简报里，一边写 `L1`（免审批），一边写 `verdict: BLOCK`（禁止写入），
自相矛盾。分级是审批/复核/门禁的唯一依据，宪法拦下就必须先回馈到等级。

现在宪法一旦 BLOCK，等级强制升到 L3，并标明"因宪法拦截而升级"，
同时保留原始等级便于追溯。

**F3 受保护路径判定失效**（打包引入）

宪法校验器内部按 `CORE_DIR.parent` 判定受保护路径。源仓库里
`Agent_OS_Core` 在根目录，基准正好是仓库根；打包成 HUB 后它挪到 `core/` 下，
基准变成 `core/`，前缀永远对不上——实测 `02_执行手册`、`.env` 全部判成 ALLOW。

在 HUB 层按包根重新判定，与校验器结论取并集。不修改宪法目录内任何文件。

**F4 图谱检索整句匹配**

原来把整句话当正则去匹配符号名（`.*Openness 硬件配置怎么改.*`），
而符号名是英文标识符，中文任务必然 0 命中。改成拆词后用"或"组合：

| 检索 | 修前 | 修后 |
|---|---|---|
| Openness 硬件配置怎么改 | 0 | 1 |

**F5 知识库多词必然 0 命中**

底层是 SQLite 全文检索，多个词默认按 AND 处理，且中文不自动分词。

| 检索 | 修前 | 修后 |
|---|---|---|
| PLC 代码 | 0 | 4 |
| 数学模型选型怎么做 | 0 | 4 |

改成三层 OR：剥虚词片段 + 该片段的 2 字滑窗 + 英文词元。

**F6 门禁轨道过滤遗漏**

quick 轨项目只该看 5 个节点，但代码遍历了全部 27 个，
把已完成的 quick 项目误报成"还差节点 1"。修正后正确显示 5/5、无待办。

**F7 包内图谱索引缺失**

主工程索引没打进包，导致同一套引擎在两个入口看到的工程数量不一致
（2 个 vs 3 个）。已补齐。

**F8 `.gitignore` 门禁路径写错**

写的 `engine/gates/manual-gates/...` 这个路径根本不存在，实际是
`skills/manual-gates/...`。规则匹配不上，244 个门禁状态文件会被误提交。
本次一并修正。

### 验证

- 环境自检 24/24
- 治理核单测 9/9
- 移植实测：整包复制到新路径后，24 项自检 + 11 个子命令 + 全流水线均通过

---

## 1.0.0 — 2026-09-15

从源仓库 `MCPCreate20260719` 拆解出的第一版可独立运行包。

包含：治理核（分级/深度策略）、记忆引擎、知识库、63 个技能、
门禁桥接、代码图谱工具。

设计取舍：

- **不打包依赖**，只用 `requirements.txt` 声明后标准安装
  （原版把 52MB 依赖塞进包，还被迫改源码适配解释器）
- **路径全配置化**，不依赖 cwd、不依赖 `parents[N]` 层级假设
- **doctor 严格只读**，不产生副作用
- **技能路由同时匹配 name 与 description**（原版只匹配英文名，中文必失配）

---

## 已知限制

以下问题**尚未修复**，使用时需注意：

| 项 | 现状 | 影响 |
|---|---|---|
| 建新索引需两个环境变量 | `CBM_INDEX_SUPERVISOR=0` + `CBM_ALLOWED_ROOT`=<仓库父目录>，必须成对给 | 中：手动建索引容易失败 |
| 记忆检索排序一般 | 用的是哈希向量（128 维），本质仍是字面匹配，不理解语义 | 中：抽象词检索准确率低 |
| 中文全文检索弱 | SQLite 默认分词器不切中文，"西门子"等词 0 命中 | 低：默认走向量检索已规避 |
| 插件固定三个位置找包 | `~/Desktop/agentos-hub`、`~/agentos-hub`、`~/Documents/agentos-hub` | 中：包放别处插件就断 |
| 离线升级脚本写死用户名 | 兜底路径含 `C:\Users\sundh\...` | 低：仅兜底分支受影响 |
