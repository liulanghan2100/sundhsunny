# 变更记录

本文件记录每个版本的**做了什么、为什么做**。
逐次提交的细节看 git 历史；这里只写"值得回看"的部分。

版本号规则：`主版本.次版本.修订号`

- **主版本**：流水线结构变化（新增加一个必经环节）
- **次版本**：新增能力或修复缺陷
- **修订号**：文案、配置、小调整

---

## 1.3.0 — 2026-09-16

落实审计报告的四项决定：补配置项、统一知识库路径、让备用能力可查、
分析审批/恢复是否接入。

### 修复

**1. 8 个失效配置项全部接上实现**

此前这些键写在 `config.yaml` 里但没人读，改它们不生效：

| 配置项 | 现在的作用 |
|---|---|
| `layers.compat` | 把任务里的旧目录名（`04_技能包`）改写成 `skills` |
| `knowledge.trusted_dir` | 传给知识库脚本，作为"已采信"存放处 |
| `knowledge.quarantine_dir` | 同上，作为"待审"存放处 |
| `skills_config.categories.shadow` | `hub.py skills --scope shadow` 按配置取名 |
| `skills_config.match_fields` | 技能路由匹配哪些字段 |
| `gates.canonical` | 门禁可用性判定（CLI + 正本目录都要在）|
| `graph.provider` | 图谱自检与错误提示里带上提供方名 |

**2. 知识库路径统一到单一真相源**

`config.yaml` 定义路径 → `hub.py` 读取后用环境变量传给脚本 → 脚本只在拿不到时才兜底。

顺带修了一个打包时引入的偏差：`kb_common.py` 用 `parents[3]` 推导包根，
算出来是 `engine/` 而不是包根，兜底路径成了不存在的 `engine/data/knowledge`。
**实测证据**：`parents[3]` = `...\agentos-hub\engine`（错），`parents[4]` = `...\agentos-hub`（对）。

**3. 副本 `skills/knowledge-base/` 同样修复**

它原本写死源仓库的 `10_知识储备库`（本包没有这个目录），且没人引用。
现在与正本一致，两份都能独立运行。

### 新增

**`hub.py mcps` 命令** —— 让 15 个备用模块可查

`engine/mcps/` 下的模块主流程不经过，但都能独立启动。
新增命令列出它们的中文用途、工具数、体积：

```
python hub.py mcps            列出全部
python hub.py mcps queue      按名字过滤
```

配套 `CAPABILITIES.md`：按"你可能想做什么"分组（任务治理/队列/验证复盘/
记忆增强/运行时编排/任务准入），并写清怎么单独启动。

**实测核查**：15 个模块**全部可导入、可建 server**。
其中 `task_queue_mcp` 需要额外的 `sys.path`（记忆引擎 + 治理核），已在文档注明。

### 修复的回归

接入 `match_fields` 时曾把中文别名挤出匹配范围，
导致搜"仓颉"从 1 个命中掉到 0 个。已修：
**别名表永远参与匹配**——像 `cangjie-thinking-distiller` 这类技能
名字和描述都是英文，中文用户只能靠别名命中。

### 分析结论（审批/恢复是否接入）

见 `AUDIT-2026-09-16.md` 第十节。结论：**审批值得接"记录+校验"部分，
恢复暂不接**——理由是 hub 是编排层，本身不执行高风险动作，没有可恢复的失败。

### 验证

17 项命令实跑全部通过（含新增的 `mcps`），自检 24/24、治理核单测 9/9。

---

## 1.2.0 — 2026-09-16

这一轮解决两件事：**改动可回溯**、**目录看得懂**。

### 新增能力

**1. 版本管理**

此前所有改动靠 `.bak` 文件手工快照——一旦文件被覆盖就找不回来。
现在改用 git，可回溯到任意时点。

仓库只跟踪文本（5.8MB / 1025 文件），排除大二进制：

| 类别 | 体积 | 是否跟踪 |
|---|---|---|
| 源码、配置、技能定义 | 5.8 MB | 是 |
| 图谱引擎 | 260 MB | 否 |
| 图谱索引 / 记忆数据 | 104 MB | 否 |
| 门禁状态（244 份） | — | 否 |

排除项的文件指纹记录在 `VERSION-MANIFEST.md`，换机器时可据此核对。

**2. `hub.py version` 命令**

不用背 git 命令：

```
hub.py version              看版本、提交历史、待提交改动
hub.py version -m "说明"     把当前改动提交，记录一次迭代
```

**3. `DIRECTORY.md` 目录说明**

逐个说明每个目录是干什么的、里面装了什么、什么情况会用到。
另含"现场指南"（想找什么去哪找）和换机器的处理步骤。

### 清理

**归位 13 个散落的备份文件**到 `_archive/2026-09-16/`，
根目录文件从 22 项减到 14 项。备份没删——记忆数据的备份不在 git 里，
是唯一的回退手段，必须留着。

**清理 26 个 `__pycache__` 目录**（63 个 `.pyc`），可随时再生成。

### 修复

**README 的目录章节过时**：原写着 `engine/gates/manual-gates`，
但实际路径早已是 `skills/manual-gates`。已改为指向 `DIRECTORY.md`。

**`.gitignore` 漏了 `_archive/`**：备份文件会被误纳入版本库，已补上。

### 审计与修复（同一轮）

对全包做了一次体检：15 个命令实跑 + 静态扫描 + 实测验证。
完整报告见 `AUDIT-2026-09-16.md`。

**发现的真漏洞（已修）**

图谱检索 `graph_search` 原本凑够条数就跳出循环，
**排在后面的项目根本没查**：

| 查询 | 修前涉及项目 | 修后 |
|---|---|---|
| Applier (limit=8) | 1 个 | 1 个 |
| Servo | 1 个 | **2 个** |
| Memory | 1 个 | **2 个** |
| Config | 1 个 | **2 个** |

主库 `MCPCreate20260719`（39430 节点）此前从未被搜到。
改为查完所有项目，结果按轮转合并，避免大项目独占名额。

**清理**

- 删图谱缓存测试残留 `*_nametest-MyProject.db`（1.72 MB）
- 图谱项目数从 4 个回到 3 个（清掉测试项目）

**报告记录的待办（未修，见报告第八节）**

- 配置项有 8 个写了但没被读取（`compat`/`trusted_dir`/`shadow` 等）
- 知识库路径三处定义不一致
- `engine/mcps` 15 个模块入口不经过（备用件，1.6MB）
- 治理核的审批/校验/恢复未接进主流程
- README 记忆条数、路径、已知边界三处过期

### 改名建议（未执行）

`skills/skills-shared-candidate/` 名字说"候选"，
**实际装着 35 个主力技能**（另有 24 个已归档、3 个待审）。

这是源库遗留的命名。改动它会牵涉技能登记表、别名表、图谱索引，
风险大于收益，因此**保持现状并在 `DIRECTORY.md` 里如实说明**。

---

## 1.1.0 — 2026-09-16

这一轮的核心是**把三个断点接上，并修掉两个安全判定的缺陷**。

### 新增能力

**1. 检索结果回传**

原来 `ask` 只给命中数量（`memory_hits: 5`），但 5 条是什么看不到，
要再手动跑一次 `recall` 才能拿到内容——检索等于白做。

现在直接返回压缩摘要（每条 160 字封顶），三个来源各自计条数、互不挤占：

```
"evidence": {
  "memory": { "hits": [{ "id": "...", "ts": "2026-09-12 14:42",
                         "summary": "Add two V21 SINAMICS V90 PN servos", "score": 0.34 }] }
}
```

**2. 技能路由汇入**

`ask` 原本完全不碰技能路由，35 个 active 技能等于隐形。

现在任务描述会反查出可用技能，写进简报和"下一步"：

| 任务描述 | 命中的技能 |
|---|---|
| 帮我审查一下这段代码的改动 | code-review |
| 做一个流程图的文档 | process-doc |
| 把这个方法蒸馏成技能 | nuwa-runbook |

中文没有分词库，用"先剥虚词、再做 n-gram"的方式近似。
（踩过的坑：直接切分会让"一下""这个"这类虚词把无关技能顶到前面。）

**3. 门禁只读预览**

`ask` 会带上门禁状态：这个项目在 27 节点手册里走到哪、下一个待办是什么。
严格只读——实测调用前后项目数都是 244，不创建目录、不写状态。

**4. 宪法层接入**

宪法校验器接进主流程，做两件事：完整性自检 + 路径写入预检。
三档判定：`BLOCK`（路径被拦）> `REVIEW`（提到受保护名字）> `ALLOW`。

**5. 图谱按路径认项目**

图谱的项目名是动态派生的（给了 `--name` 就是那个名字，没给就是绝对路径
slug 化，如 `C-Users-sundh-AppData-Local-Temp-_nametest-MyProject`）。
换机器、换目录，名字就变，写死必废。

新增解析层，允许用**路径**或**名字片段**指定项目：

| 输入 | 解析结果 |
|---|---|
| `C:/Users/.../MCPCreate20260719` | MCPCreate20260719 |
| `.../MCPCreate20260719/AutoRobot` | 那个子项目 |
| `OpenNiss` | OpenNiss.TiaExport.V21 |
| `mcpcreate20260719` | MCPCreate20260719（大小写不敏感）|

### 缺陷修复

**F1 中文动词表缺字**（安全相关）

`帮我改 charter.json 的红线` 判成 L1（免审批）。根因是词表只有双字词
（修改/编辑），漏了口语里更常用的单字"改"、"删"。

修复后：

| 任务 | 改前 | 改后 |
|---|---|---|
| 帮我改 charter.json 的红线 | L1 | **L2** (write) |
| 把 charter.json 删掉 | L1 | **L3** (delete) |
| 替换 charter.json 里的内容 | L1 | **L2** (write) |

**F2 宪法判定不回馈到分级**（安全相关）

同一份简报里，一边写 `L1`（免审批），一边写 `verdict: BLOCK`（禁止写入），
自相矛盾。分级是审批/复核/门禁的唯一依据，宪法拦下就必须先回馈到等级。

现在宪法一旦 BLOCK，等级强制升到 L3，并标明"因宪法拦截而升级"，
同时保留原始等级便于追溯。

**F3 受保护路径判定失效**（打包引入）

宪法校验器内部按 `CORE_DIR.parent` 判定受保护路径。源仓库里
`Agent_OS_Core` 在根目录，基准正好是仓库根；打包成 HUB 后它挪到 `core/` 下，
基准变成 `core/`，前缀永远对不上——实测 `02_执行手册`、`.env` 全部判成 ALLOW。

在 HUB 层按包根重新判定，与校验器结论取并集。不修改宪法目录内任何文件。

**F4 图谱检索整句匹配**

原来把整句话当正则去匹配符号名（`.*Openness 硬件配置怎么改.*`），
而符号名是英文标识符，中文任务必然 0 命中。改成拆词后用"或"组合：

| 检索 | 修前 | 修后 |
|---|---|---|
| Openness 硬件配置怎么改 | 0 | 1 |

**F5 知识库多词必然 0 命中**

底层是 SQLite 全文检索，多个词默认按 AND 处理，且中文不自动分词。

| 检索 | 修前 | 修后 |
|---|---|---|
| PLC 代码 | 0 | 4 |
| 数学模型选型怎么做 | 0 | 4 |

改成三层 OR：剥虚词片段 + 该片段的 2 字滑窗 + 英文词元。

**F6 门禁轨道过滤遗漏**

quick 轨项目只该看 5 个节点，但代码遍历了全部 27 个，
把已完成的 quick 项目误报成"还差节点 1"。修正后正确显示 5/5、无待办。

**F7 包内图谱索引缺失**

主工程索引没打进包，导致同一套引擎在两个入口看到的工程数量不一致
（2 个 vs 3 个）。已补齐。

**F8 `.gitignore` 门禁路径写错**

写的 `engine/gates/manual-gates/...` 这个路径根本不存在，实际是
`skills/manual-gates/...`。规则匹配不上，244 个门禁状态文件会被误提交。
本次一并修正。

### 验证

- 环境自检 24/24
- 治理核单测 9/9
- 移植实测：整包复制到新路径后，24 项自检 + 11 个子命令 + 全流水线均通过

---

## 1.0.0 — 2026-09-15

从源仓库 `MCPCreate20260719` 拆解出的第一版可独立运行包。

包含：治理核（分级/深度策略）、记忆引擎、知识库、63 个技能、
门禁桥接、代码图谱工具。

设计取舍：

- **不打包依赖**，只用 `requirements.txt` 声明后标准安装
  （原版把 52MB 依赖塞进包，还被迫改源码适配解释器）
- **路径全配置化**，不依赖 cwd、不依赖 `parents[N]` 层级假设
- **doctor 严格只读**，不产生副作用
- **技能路由同时匹配 name 与 description**（原版只匹配英文名，中文必失配）

---

## 已知限制

以下问题**尚未修复**，使用时需注意：

| 项 | 现状 | 影响 |
|---|---|---|
| 建新索引需两个环境变量 | `CBM_INDEX_SUPERVISOR=0` + `CBM_ALLOWED_ROOT`=<仓库父目录>，必须成对给 | 中：手动建索引容易失败 |
| 记忆检索排序一般 | 用的是哈希向量（128 维），本质仍是字面匹配，不理解语义 | 中：抽象词检索准确率低 |
| 中文全文检索弱 | SQLite 默认分词器不切中文，"西门子"等词 0 命中 | 低：默认走向量检索已规避 |
| 插件固定三个位置找包 | `~/Desktop/agentos-hub`、`~/agentos-hub`、`~/Documents/agentos-hub` | 中：包放别处插件就断 |
| 离线升级脚本写死用户名 | 兜底路径含 `C:\Users\sundh\...` | 低：仅兜底分支受影响 |
